import React from "react";
import {
  velocityPerFrame,
  velocityPerSecond,
  PhysicsSpringOptions,
  interpolate,
  inertia,
  attract,
  attractExpo,
  createAttractor,
  cubicBezier,
} from "popmotion";

const AnimatePhysics = () => {
  // animate a rounded ball div with the inertia
  
  return <div>AnimatePhysics</div>;
};

export default AnimatePhysics;
